/** The last legacy route now uses the shared light visual language.
 * Keep the root-layout integration stable; grain belongs to local artwork,
 * never a fixed layer over text, controls or the interactive map. */
export function PaperOverlayGate() {
  return null;
}
